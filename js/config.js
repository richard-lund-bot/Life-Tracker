/* Spor — sync configuration.
 * Fill in your Supabase project's URL and publishable (anon) key to enable sync;
 * see docs/supabase-setup.md. Both values are safe to commit — data access is
 * protected by Row Level Security, not by this key. Leave empty to run
 * purely local (everything else works without it). */

const SPOR_CONFIG = {
  supabaseUrl: '',   // e.g. 'https://abcdefghij.supabase.co'
  supabaseKey: '',   // 'sb_publishable_…' (or legacy anon key)
};
